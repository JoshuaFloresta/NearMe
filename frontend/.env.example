VITE_API_URL=https://your-nearme-api.up.railway.app

# Set to true only when your backend runtime supports Socket.IO (e.g. Railway).
# In production, realtime defaults to disabled unless explicitly set to true.
VITE_REALTIME_ENABLED=false
